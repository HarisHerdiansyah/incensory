import { LoginForm } from '@/components/auth';

export default function Login() {
  return (
    <main id='login' className='min-h-screen flex items-center justify-center'>
      <LoginForm />
    </main>
  );
}
